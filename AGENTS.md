# AGENTS.md — CRO仪器排程系统

## 技术栈
- 后端: Python 3.12 + FastAPI + SQLAlchemy + MySQL
- 前端: Vue 3 + TypeScript + Ant Design Vue 4 + 图表组件 + vis-timeline
- 排程引擎: OR-Tools CP-SAT

---

## 后端规则

### 分层架构
```
api/        → 路由层：解析请求、调用一个 service、返回响应
services/   → 业务层：编排逻辑，不 import FastAPI/HTTPException
models/     → 数据层：SQLAlchemy ORM 模型
schemas/    → 接口层：Pydantic 请求/响应模型
core/       → 配置层：config + database 连接
```

### 路由层 (api/)
- 每个 handler ≤ 15 行
- 只做三件事：解析输入 → 调用 service → 返回响应
- 不在路由里写 SQL 或业务逻辑
- 每个端点声明 `response_model=`

### 业务层 (services/)
- 不 import `fastapi.HTTPException`
- 不 import `sqlalchemy`（那是 models 的事）
- 抛业务异常，由路由层转 HTTP 状态码

---

## 前端规则

### 组件
- 使用 Vue SFC（`.vue`）和 `<script setup lang="ts">`
- 状态尽量靠近使用处，不提前提升
- 派生状态用 `computed`，副作用用 `watch` / 生命周期钩子
- 提取重复逻辑到 composable（`useXxx.ts`）

### TypeScript
- 对象定义用 `interface`，联合类型用 `type`
- 禁止 `any`，用 `unknown` 或具体类型
- Props 接口以 `Props` 结尾（如 `GanttProps`）
- `tsconfig.json` 保持 `strict: true`

### 命名
- 组件文件: PascalCase（`InstrumentGantt.vue`）
- 工具/composable 文件: camelCase（`useSchedule.ts`）
- 变量/函数: camelCase，布尔值加 `is/has` 前缀

### 状态管理
- 单页面状态用 `ref` / `reactive`
- 跨组件共享用 provide/inject 或轻量 composable（不引入 Redux）
- API 请求结果直接存在调用组件中

---


# 项目通用开发规范（Global AI Coding Rules）

本规范适用于本仓库根目录及全部子目录。参与本项目开发的 AI Agent 必须遵守以下规则。

## 1. 代码体积与文件拆分（File Size & Modularization）

- 【硬性上限】单个文件不得超过 600 行代码。
- 【拒绝巨无霸文件】当一个文件功能变多时，必须按照功能拆分到不同子目录，例如 UI 组件、服务函数和类型定义。
- 【职责单一】每个文件只做一件事。UI 页面只负责布局与状态调度，复杂业务逻辑必须抽离到独立的 service 或 helper 函数中。
- 修改代码后必须检查相关文件的行数；发现超限时，应先完成合理拆分，不能继续向超限文件堆叠代码。

### 函数编写规范（Function Rules）

- 【职责单一与按需拆分】衡量函数好坏的标准是职责是否单一，而非机械统计行数。
- 【主函数仅作流程调度】主函数仅按顺序编排步骤，具体数据处理、格式校验等细节必须下沉至独立子函数。
- 【拒绝多重职责】禁止编写兼揽多项业务的函数。若函数名称或逻辑中包含 `And` 或“与”（例如 `fetchAndParseData`），必须拆分为独立的原子函数。
- 【按需弹性行数】纯逻辑或工具函数建议保持精简；UI 渲染组件与复杂业务 Handler 在确保职责单一的前提下，允许保留完整上下文。
- 【不允许兜底行为】条件不满足就返回错误，不允许自我猜测并加入兜底代码。


## 2. 前后端数据与接口对齐（API & Data Consistency）

- 【数据驱动】前端必须严格根据后端返回的数据结构或配置（Metadata）动态渲染，严禁在前端硬编码业务提取规则。
- 【单点真实源】所有 API 请求与数据类型定义必须在统一目录（如 `services/`、`api/` 或 `types/`）下管理，禁止在组件内部散落 `fetch` 或 `axios` 请求。
- 前后端字段名称、枚举值、可空性和错误结构必须保持一致；调整接口时必须同步更新类型定义和调用方。

## 3. 稳健性与错误捕获（Robustness & State）

- 【状态完整】所有异步请求必须显式处理加载中（Loading）、成功（Success）和失败（Error）三种状态。
- 【用户友好】所有异常必须被捕获，并返回清晰的中文错误提示；严禁出现页面无响应、无提示或仅在控制台报错的“假死”现象。
- 【关键日志】数据提取、接口调用、报告生成、模板编译、ONLYOFFICE 通信等关键业务节点必须保留必要的前端 console 或后端日志，便于定位问题。
- 日志不得输出密码、Token、密钥、完整个人敏感信息或受保护的原始数据。

## 4. 增量修改原则（Incremental Changes）

- 【禁止破坏性重构】添加功能或修复 Bug 时只修改目标模块，严禁随意重写、删减或格式化无关代码。
- 【保留调试入口】关键业务逻辑必须保留必要的日志和可验证入口，不得为了表面整洁删除排障能力。
- 【尽量少新增依赖】未经用户明确允许，严禁新增第三方 npm 或 pip 依赖；优先使用标准库和项目已有工具。
- 必须保留用户工作区中的既有修改，不得覆盖、回滚或删除与当前任务无关的变更。

## 5.架构设计与通用性（Architecture & Generalization）
【单一真实源（SSOT）】 任何业务规则或元数据仅允许存在一处权威定义。严禁在不同模块、数据库表或配置文件中重复存储相同含义的配置。

【严格职责隔离】 严禁跨层级承载逻辑。数据解析层仅关注“数据获取”，视图/渲染层仅关注“数据呈现”；渲染层或模板映射不得包含任何数据提取与来源判断逻辑。

【确定性与 Fail-Fast】 业务执行必须具备确定性。遇到配置冲突、状态不一致或数据缺失时，必须立即显式抛出明确异常；严禁编写隐式降级（Fallback）或猜测逻辑掩盖配置缺陷。

【拒绝特例硬编码】 通用业务流程必须通过标准抽象或配置驱动。严禁在通用代码中为特定的业务实体、特定字段或特定标识硬编码条件分支（if-else）。

【根因治理优先】 修复缺陷时必须追溯架构根因。若问题源于规则重叠或结构冲突，必须优先清理与重构底层结构，严禁通过叠加局部分支或补丁代码掩盖架构问题。

## 6. 修改后的最低验证要求

- 前端修改至少运行 TypeScript 检查或生产构建。
- 后端修改至少运行目标模块测试；没有对应测试时，应执行最接近的现有测试或只读验证。
- 提交结果前运行差异检查，确保没有空白错误、冲突标记或意外修改。
- 如果由于环境、权限或外部服务导致无法验证，必须明确说明未验证项目、原因和用户可执行的验证方式。
-回复用户的时候不要用编号，例如任务176需要说明是XX项目的XX任务

## 7. Python 虚拟环境与测试命令

- 项目虚拟环境 `.venv` 已包含完整后端依赖：PyMySQL 1.1.1、NumPy 2.4.6、Pandas 3.0.3、OR-Tools 9.15.6755，且 `pip check` 无依赖冲突。
- 禁止直接运行全局 `pytest`、`python` 或 Anaconda 环境中的测试命令。全局环境中的旧 PyArrow 等二进制扩展与 NumPy 2 不兼容，会造成与项目代码无关的测试收集错误。
- 后端测试统一从仓库根目录使用项目虚拟环境执行：

```bash
cd server
../.venv/bin/python -m pytest
```

- 运行指定测试时同样使用 `../.venv/bin/python -m pytest tests/<test_file>.py`。
- 若出现依赖导入或二进制兼容错误，必须先确认命令使用的是项目 `.venv`，不得据此误判项目依赖缺失，也不得擅自修改全局 Anaconda 环境。
